const useMath = () => {
  const mapScores = (tipo) => {
    console.log(tipo);

    return <p>Esto es una prueba de lo que se renderiza</p>;
  };
  return {
    mapScores,
  };
};

export default useMath;
